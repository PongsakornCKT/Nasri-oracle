# Session Retrospective — 2026-03-12

> Evening review. Short session, foundational work.

## What Happened

- **Cloned oracle-v2** from `Soul-Brews-Studio/oracle-v2` into the workspace
- **Installed dependencies** — 181 packages via `bun install` (Hono, Drizzle, LanceDB, MCP SDK, Qdrant, sqlite-vec)
- This is Nasri's first encounter with the oracle-v2 codebase — the MCP Memory Layer that powers the Oracle system

## What Was Learned

- **oracle-v2 is substantial**: full MCP server with semantic search, vector DB (LanceDB + Qdrant), SQLite via Drizzle, HTTP server (Hono), CLI, forum system, vault, dashboard UI, process manager
- **v0.4.0-nightly** — active development, not yet stable
- **Bun-first**: requires Bun >=1.2.0, uses Bun's native SQLite bindings alongside better-sqlite3
- The repo contains its own `CLAUDE.md` with project-specific instructions

## What Surprised

- oracle-v2 has a nested `oracle-v2/` directory inside itself — worth investigating
- It includes a `TIMELINE.md` — likely chronicles the project's evolution
- Has both e2e tests (Playwright) and unit tests (Vitest) — well-structured for a nightly build
- A file `oracle.db.MIGRATED-TO-HOME.md` suggests the DB moved to `$HOME` — important for setup

## What's Next

- **Read oracle-v2's CLAUDE.md** — understand its rules before working in it
- **Explore the source architecture** — map tools/, server/, vector/, vault/, forum/
- **Understand the MCP integration** — how oracle-v2 exposes memory tools to Claude
- **Check if oracle-v2 needs configuration** (DB path, API keys, vector DB setup)
- **Consider how oracle-v2 relates to Nasri's ψ/ brain** — are they complementary or overlapping?
